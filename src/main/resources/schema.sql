DROP SCHEMA IF EXISTS Billing_Engine;

CREATE SCHEMA Billing_Engine;
USE Billing_Engine;

CREATE TABLE INVOICE (ID bigint GENERATED BY DEFAULT AS IDENTITY,
CLIENT varchar(100),
VAT_RATE bigint,
INVOICE_DATE date,
PRIMARY KEY (ID));

CREATE TABLE LINE_ITEMS (ID bigint GENERATED BY DEFAULT AS IDENTITY,
QUANTITY bigint, DESCRIPTION varchar(150),
UNIT_PRICE float,
INVOICE_ID bigint NOT NULL,
PRIMARY KEY (ID));
ALTER TABLE LINE_ITEMS ADD CONSTRAINT FKLINE_ITEMS855268 FOREIGN KEY (INVOICE_ID) REFERENCES INVOICE (ID);
